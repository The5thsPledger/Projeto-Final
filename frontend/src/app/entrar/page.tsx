import Layout from '@/components/Layout';
import FormLogin from '@/components/FormLogin';

export default function LoginPage() {
  return (
    <Layout titulo="Entrar no sistema">
      <FormLogin />
    </Layout>
  );
}
